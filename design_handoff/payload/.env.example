# Database — demo connection (as provided)
DATABASE_URL=mongodb://127.0.0.1/payload-demo

# Payload secret — used to sign auth tokens. Replace before production.
PAYLOAD_SECRET=replace-with-a-long-random-string
