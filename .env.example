# MongoDB connection (demo)
DATABASE_URL=mongodb://127.0.0.1/payload-demo

# Payload secret — used to sign auth tokens. Replace with a long random string.
PAYLOAD_SECRET=replace-with-a-long-random-string
